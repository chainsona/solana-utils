{
  "name": "solana-utils-root",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist/secret-key && cp public/index.html dist/index.html && cp secret-key/html/index.html dist/secret-key/index.html"
  }
}
